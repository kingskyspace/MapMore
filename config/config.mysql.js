'use strict';

exports.mysql = {
  // 单数据库信息配置
  client: {
    // host
    host: '45.32.84.250',
    // 端口号
    port: '3306',
    // 用户名
    user: 'tone',
    // 密码
    password: '786384149aa',
    // 数据库名
    database: 'love_info',
  },
  // 是否加载到 app 上，默认开启
  app: true,
  // 是否加载到 agent 上，默认关闭
  agent: false,
};
